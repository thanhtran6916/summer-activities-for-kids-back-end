server.port=8085

## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=${SECRETS_DB_URL:jdbc:postgresql://171.253.57.135:7200/summer_activity}
spring.datasource.username=${SECRETS_DB_USERNAME:postgres}
spring.datasource.password=${SECRETS_DB_PASSWORD:app12345}

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

#Multipart file
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Authentication
auth.secret.key=${AUTH.SECRET.KEY:1234567890}
auth.expire.time=${AUTH.EXPIRE.TIME:86400000}

# Keycloak
keycloak.realm=${SECRETS_KEYCLOAK_REALM:summer-activity}
keycloak.auth-server-url=${SECRETS_KEYCLOAK_URL:https://dangkykhoahe.online/auth}
keycloak.resource=${SECRETS_KEYCLOAK_RESOURCE:login-app}
keycloak.ssl-required=external
keycloak.principal-attribute=preferred_username
keycloak.use-resource-role-mappings=true
keycloak.bearer-only=true
keycloak.cors=true
keycloak.credentials.secret=${SECRETS_KEYCLOAK_SECRET:jfUl2p6CB3uEKZdBnlwTBFdQTfqndIul}
